/** @type {import('next').NextConfig} */
const nextConfig = {
  // Since we want to use the root .html files as the main entry point,
  // we'll keep the Next.js part as a separate feature if needed.
};

export default nextConfig;
